import { createSelector } from 'reselect';

// const selectNews = (state) => state.get('news');
//
// export const getHeadlines = () =>
//   createSelector(selectNews, (newsState) =>
//     newsState.get('headlines').toJS()
// );
